import { Mocker } from '@hexcore/core';
import { createMock } from 'ts-auto-mock';
import { <%=it.name%>CommandHandler } from '@app/<%=it.moduleImportName%>/Application/Command/<%=it.group%>/<%=it.name%>/<%=it.fullname%>CommandHandler';
import { <%=it.name%>Command } from '@app/<%=it.moduleImportName%>/Application/Command/Post/<%=it.name%>/<%=it.fullname%>Command';

/**
 * @group unit/<%=it.moduleImportName%>
 */
describe('Command.<%=it.group%>.<%=it.name%>', () => {
  let handler: <%=it.fullname%>CommandHandler;

  beforeEach(() => {
    handler = new <%=it.fullname%>CommandHandler();
  });

  test('execute()', async () => {
    const command = new <%=it.fullname%>Command();

    await handler.execute(command);
    fail('Need implementation');
  });
});
